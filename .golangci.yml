version: "2"

run:
  timeout: 5m

linters:
  default: standard
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
      - source: "^\\s*defer "
        linters:
          - errcheck

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
